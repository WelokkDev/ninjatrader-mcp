import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

/**
 * Register draw_zone tool.
 * Writes a draw command to SQLite for the NinjaScript indicator to pick up
 * and render as a rectangle on the chart.
 */
export function registerDrawZone(_server: McpServer): void {
  // Will be implemented
}
