import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

/**
 * Register clear_zones tool.
 * Writes clear commands to SQLite, causing the NinjaScript indicator
 * to remove drawn zone rectangles from the chart.
 */
export function registerClearZones(_server: McpServer): void {
  // To be done
}
